<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    {{!-- TODO: 正式上線前 timestamp 記得拿掉 --}}
    <link rel='stylesheet' href='/stylesheets/style.css?ver={{timestamp}}' />
  </head>
  <body>
    {{> mynav }}
    <div class="container">
      <div class="message-box {{#if messageBOX}}show{{/if}} text-center">
        <p>
            {{messageBOX.errMsg}}
            {{messageBOX.succMsg}}
            {{messageBOX.loginMsg}}
            {{messageBOX.loggedMsg}}
        </p>
        {{#if messageBOX.errMsg}} 
          <button class="btn btn-dark" id="close-msgBOX">嗚嗚，換一個試試</button>
        {{/if}}
        {{#if messageBOX.succMsg}} 
          <a class="btn btn-dark" href="/signIn">OK</a>
        {{/if}}
        {{#if messageBOX.loginMsg}} 
          <button class="btn btn-dark" id="close-msgBOX">哭哭</button>
        {{/if}}
        {{#if messageBOX.loggedMsg}} 
          <a class="btn btn-dark" href="/">OK</a>
        {{/if}}
      </div>
      {{{body}}} 
    </div>
    {{> footer }}
  </body>
  <script src="https://code.jquery.com/jquery-3.6.0.min.js" integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4=" crossorigin="anonymous"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
  {{{_sections.script}}}
  <script>
    // 關閉訊息提醒視窗
    $('#close-msgBOX').click(() => {
      $('.message-box').removeClass('show');
    });
    // navbar 的 cart 狀態
    let cart = JSON.parse(localStorage.getItem('cart')) || [];
    if (cart.length > 0) {
      $('#added-inform').addClass('cart-active');
    }
  </script>
</html>
